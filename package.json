{
  "name": "4estfilms-tooling",
  "private": true,
  "type": "module",
  "description": "Repo-level tooling. The site itself lives in site/ and has its own package.json — nothing here ships to the Worker or is installed by CI.",
  "scripts": {
    "og": "node scripts/og.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
